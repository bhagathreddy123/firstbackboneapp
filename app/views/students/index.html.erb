<div id="students"></div>

<script type="text/javascript">
  $(function() {
    // Firstbackboneapp is the app name
    window.router = new Firstbackboneapp.Routers.PostsRouter({students: <%= @students.to_json.html_safe -%>});
    Backbone.history.start();
  });
</script>


<!--
<p id="notice"><%= notice %></p>

<h1>students</h1>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th>email</th>
      <th>Mobile</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.name %></td>
        <td><%= student.email %></td>
        <td><%= student.mobile %></td>
        <td><%= link_to 'Show', student %></td>
        <td><%= link_to 'Edit', edit_student_path(student) %></td>
        <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New student', new_student_path %>

